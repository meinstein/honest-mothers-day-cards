// local
import { fonts, colors } from '../../styles'

export default {
	container: {
		display: 'flex',
		flexDirection: 'column',
		alignItems: 'center',
		fontFamily: fonts.OHNO,
		color: colors.RED
	},
	img: {
		height: 'auto',
		width: '100%',
		minWidth: 200,
		maxWidth: 600
	}
}
