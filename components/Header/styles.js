// local
import { fonts, colors } from '../../styles'

export default {
	container: {
		display: 'flex',
		flexDirection: 'column',
		alignItems: 'center',
		fontFamily: fonts.OHNO,
		color: colors.RED
	},
	img: {
		width: '100%',
		maxWidth: 400
	}
}
