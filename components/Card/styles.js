import { colors, fonts } from '../../styles'

export default {
	card: {
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
		fontFamily: fonts.OHNO,
		backgroundColor: colors.ORANGE,
		color: colors.RED,
		fontSize: 48,
		height: 450,
		textAlign: 'center',
		padding: 24
	}
}
